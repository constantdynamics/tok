package app.tok.ui.settings

import androidx.compose.runtime.Composable
import app.tok.ui.components.CenteredMessage

// Stub — wordt in fase 5d vervangen door instellingen (modellen, apparaten, sync).
@Composable
fun SettingsScreen() {
    CenteredMessage("Instellingen — binnenkort")
}
